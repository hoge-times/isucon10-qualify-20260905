# ブラウザ操作ログ

2回の試行（20260905-trial-01、20260905-trial-02）の操作記録を1つにまとめたもの。分析結果は [report.md](report.md) を参照。

## 共通の前提

- 実施日：2026-09-05 JST。時刻の個別計測はしていないため、操作順を連番で記録。
- 対象：http://54.168.109.155/ （ISUCON10予選の練習環境、ISUUMO）
- 手段：Codexブラウザエージェント（CUA）。画面のアクセシビリティ情報で遷移と結果を確認。
- テスト入力のメールアドレスはすべて `example.invalid` ドメイン。実在の個人情報は使用していない。
- サーバー設定・コード変更、初期化、ベンチマーク実行は行っていない。

---

# 20260905-trial-01：条件検索からの購入と、おすすめ物件への資料請求

- 範囲：2通りのユーザーストーリー（US-01、US-02）。購入1回、資料請求1回。
- テスト入力：`browser-story-01@example.invalid`、`browser-story-02@example.invalid`。
- ローカル実装の参照時HEAD：`9079187eee8880a5f1a85d95f62f3745b45a17bd`。稼働サーバーとの完全一致は未検証。

## 事前調査

| 順 | 操作 | 結果・判断 |
|---|---|---|
| P01 | `docs/` の4資料を読む | `isucon10_qualify_manual.md`、`kickoff.md`、`matsuu-isucon10-qualify-README.md`、`SOURCES.md` を確認。 |
| P02 | 採点仕様を確認 | スコア＝イス購入件数＋物件資料請求件数−減点。ベンチはAPIにのみアクセス。 |
| P03 | ブラウザでトップを開く | 「イス検索」「物件検索」「なぞって検索」「最安の物件」「最安のイス」を確認。 |

## 実行1（US-01）：条件検索からイスを購入

| 順 | 画面・操作 | 観測結果 | 直接加点との関係 |
|---|---|---|---|
| A01 | トップの「イス検索」をクリック | `/chair/search/` に遷移。高さ・横幅・奥行き・価格・色・種類・特徴の指定欄あり。条件未指定時のSearchは無効。 | 検索準備。直接加点なし。 |
| A02 | 価格「〜3000円」、種類「ゲーミングチェア」、色「黒」を選択 | 選択状態を確認。Searchが有効化。その他の条件は未指定。 | 直接加点なし。 |
| A03 | Searchをクリック | 結果20件を表示。ページ1〜4へのボタンあり。先頭は「【本格仕様】社長のイス」1865円。 | 候補発見。直接加点なし。 |
| A04 | 「Go to page 2」をクリック | page 2の表示に変更。先頭が「【本格仕様】金のダイニングチェア」1089円に変わる。 | 比較・ページング。直接加点なし。 |
| A05 | その先頭のイスをクリック | `/chair/detail/?id=10169`。価格1089円、黒、ゲーミングチェアで条件と一致。縦144cm・横151cm・奥74cm。「購入」フォームと「このイスにオススメの物件」を確認。 | 購入前の確認。直接加点なし。 |
| A06 | Email addressへ `browser-story-01@example.invalid` を入力、Submitを1回クリック | 同じ詳細画面に新しく `200` を表示。 | 購入成功。ベンチ走行中に同種の成功が計上されれば購入件数+1。 |

購入後の画面抜粋（ブラウザ観測）：

```text
URL: http://54.168.109.155/chair/detail/?id=10169
heading 購入:
text field browser-story-01@example.invalid
button Submit
text 200
名前: 【本格仕様】金のダイニングチェア
値段: 1089 円 色: 黒 種類: ゲーミングチェア
heading このイスにオススメの物件:
button 坂本 ISUマンション ... 家賃: 34844円
```

時刻チェックポイント：A05の後、A06の前にローカル時計で `2026-09-05T14:00:44+0900` を確認。

## 実行2（US-02）：イスにおすすめの物件から資料請求

実行1のイス詳細を出発点にした連続した利用。購入済みであることが推薦表示の必要条件だとは判断していない（推薦は購入前にも表示されていた）。

| 順 | 画面・操作 | 観測結果 | 直接加点との関係 |
|---|---|---|---|
| B01 | イス詳細のおすすめ一覧から「坂本 ISUマンション」家賃34844円をクリック | `/estate/detail/?id=7045` に遷移。 | イスから物件への導線。推薦・詳細表示自体は直接加点なし。 |
| B02 | 物件詳細を確認 | 賃料34844円、ドア縦87cm・横193cm、特徴「高齢者歓迎」、資料請求フォームを確認。地図のZoom in/out操作欄も存在。 | 資料請求前の確認。直接加点なし。 |
| B03 | Email addressへ `browser-story-02@example.invalid` を入力、Submitを1回クリック | 同じ詳細画面に新しく `200` を表示。 | 資料請求成功。ベンチ走行中に同種の成功が計上されれば資料請求件数+1。 |

資料請求後の画面抜粋（ブラウザ観測）：

```text
URL: http://54.168.109.155/estate/detail/?id=7045
heading 資料請求:
text field browser-story-02@example.invalid
button Submit
text 200
名前: 坂本 ISUマンション
賃料: 34844 円
heading ドアのサイズ
縦 (cm) : 87
横 (cm) : 193
```

## 終了時の照合

| 順 | 確認 | 結果 |
|---|---|---|
| V01 | 手元のfrontendの購入・資料請求ハンドラーを読む | どちらも送信先APIの `response.status` と本文を画面へ表示する実装。観測した200を成功応答と解釈する裏付け。通信キャプチャ自体は採取していない。 |
| V02 | 手元のGo実装を読む | 購入は在庫のあるイスをロックし在庫を1減らしてコミット、成功時200。資料請求はemailの型と物件存在を確認し200。 |
| V03 | 時刻を確認 | 両シナリオ終了後の照合時刻は `2026-09-05T14:01:27+0900`。 |

20260905-trial-01 の実行結果：購入成功表示1回、資料請求成功表示1回。ベンチのpass、点数増分、DBの変更前後、メールの到達は測定していない。

---

# 20260905-trial-02：物件条件検索・地図・最安一覧の4経路

- 範囲：4通りのユーザーストーリー（US-03〜US-06）。購入1回、資料請求3回の200表示。メール入力エラー1回。検索結果なしからの復帰1回。
- テスト入力：`browser-story-03@example.invalid` 〜 `browser-story-06@example.invalid`。
- 手段の補足：1タブで順次実行。画面のアクセシビリティ情報に加え、地図のスクリーンショットを観測。
- 参照元：前回の資料とローカル実装。追加調査用ブランチの起点はマージ済み `origin/main`（`6ff602d`）。
- 購入・資料請求は練習用データで実行。

## US-03：物件の条件を調整して資料請求

| 順 | 操作 | 画面上の観測 |
|---|---|---|
| C01 | トップ → 物件検索 | `/estate/search/`。ドア横幅・高さ・賃料・特徴を指定できる。初期状態はSearch無効。 |
| C02 | 賃料「〜50000円」、特徴「エアコン付き」「インターネット無料」を選択 | 3条件の選択とSearch有効化を確認。 |
| C03 | Search | 2件表示。「藤原 ISUビル」48347円、「伊藤 ISUビル」45672円。page 1、前後ページ無効。 |
| C04 | 「バス・トイレ別」を追加してSearch | 2件のカードとpage 1が消え、前後ページボタンは無効。観測したツリー内には結果なしの説明文は見られなかった。 |
| C05 | 「バス・トイレ別」を解除してSearch | 元の2件が再表示。 |
| C06 | 安い方の「伊藤 ISUビル」を選択 | `/estate/detail/?id=6868`。45672円、特徴「フローリング エアコン付き インターネット無料」、ドア104×190cm。 |
| C07 | `not-an-email` を入力しSubmit | `Invalid email address format.` を表示。成功表示なし。 |
| C08 | `browser-story-03@example.invalid` に修正しSubmit | 表示が `200` に変わる。資料請求成功。 |

成功観測の抜粋：

```text
URL: /estate/detail/?id=6868
text field browser-story-03@example.invalid
text 200
```

## US-04：なぞって検索から資料請求

| 順 | 操作 | 画面上の観測 |
|---|---|---|
| D01 | トップ → なぞって検索 | `/estate/nazotte/`。関東の地図、Zoom in/out、名前のないボタンを確認。 |
| D02 | スクリーンショットで地図下部の丸い緑の手指アイコンを確認しクリック | ボタンにフォーカス。モード名の文字表示はない。 |
| D03 | 地図を画面座標 `(220,380)` → `(440,500)` へドラッグ | 地図内に画像要素5つが追加。スクリーンショットで青い線・領域とマーカーを確認。 |
| D04 | `(249,376)` 付近のマーカーをクリック | 「ISU高橋レジデンス」のポップアップリンクが表示。リンク先は `/estate/detail/?id=24964`。 |
| D05 | 物件名リンクをクリック | 詳細表示。賃料93933円、ドア67×197cm、特徴「ロフト ワンルーム 即入居可」。 |
| D06 | `browser-story-04@example.invalid` を入力しSubmit | `200` を表示。資料請求成功。 |

座標は当時の663×784pxの画面に対する値。別の画面サイズで固定値の再利用はできない。正確な緯度経度、多角形の頂点、境界判定は採取していない。地図の表示位置とダミー住所の行政区分の一致も検証していない。

成功観測の抜粋：

```text
URL: /estate/nazotte/
link ISU高橋レジデンス → /estate/detail/?id=24964
URL: /estate/detail/?id=24964
text field browser-story-04@example.invalid
text 200
```

## US-05：最安のイスから購入

| 順 | 操作 | 画面上の観測 |
|---|---|---|
| E01 | トップに戻り最安イスを名前で指定してクリックを試行 | ブラウザ操作ツールが要素を見つけられず3秒でタイムアウト。アプリのHTTPエラーとは判定しない。 |
| E02 | 最新の画面情報を取得 | 「【お買い得】モダンなダイニングチェア」1000円が存在。新しい要素番号でクリック成功。 |
| E03 | 詳細を確認 | `/chair/detail/?id=10831`。1000円、青、ゲーミングチェア、縦40・横60・奥157cm。 |
| E04 | `browser-story-05@example.invalid` を入力しSubmit | `200` を表示。購入成功。 |

成功観測の抜粋：

```text
URL: /chair/detail/?id=10831
text field browser-story-05@example.invalid
text 200
```

## US-06：最安の物件から資料請求

| 順 | 操作 | 画面上の観測 |
|---|---|---|
| F01 | トップの最安物件一覧で先頭の「松本 ISUビル」を選択 | `/estate/detail/?id=7087`。30001円、ドア64×81cm、特徴「敷地内ゴミ置場」。 |
| F02 | `browser-story-06@example.invalid` を入力しSubmit | `200` を表示。資料請求成功。 |

成功観測の抜粋：

```text
URL: /estate/detail/?id=7087
text field browser-story-06@example.invalid
text 200
```

## 補足照合と限界

- ローカル `frontend/src/components/NazotteMap.tsx` を確認。ドラッグ中の座標から凸包を作り、閉じた座標列を `POST /api/estate/nazotte` に送る。結果を受けてマーカーを表示し、モードをdragに戻す実装。
- ローカル資料請求UIを確認。メール形式エラーはfetchより前でreturnする。HTTPエラーやベンチの減点とは別の分岐。
- ローカル時計の確認値：`2026-09-05T14:17:04+0900`（F01後、F02前のコード照合時）。
- スクリーンショットは操作中の観測に使用し、画像ファイルとしては保存していない。上の抜粋は観測結果から転記したログ。
- 通信キャプチャ、DB更新前後、メール配送、ベンチのpass/スコアは未計測。
